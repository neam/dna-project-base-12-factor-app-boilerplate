# Codeception Test Suite Configuration

# suite for unit-db-agnostic (internal) tests.
# RUN `build` COMMAND AFTER ADDING/REMOVING MODULES.

class_name: DbAgnosticCodeGuy
modules:
    enabled: [CodeHelper]
gherkin:
    contexts:
        default:
